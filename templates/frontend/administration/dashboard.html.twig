{% extends 'base.html.twig' %}

{% block title %}{{ 'administration.dashboard'|trans }}{% endblock %}

{% block body %}
    <section class="intro">
        <div class="container">
            <h1 class="intro__title">{{ 'administration.dashboard'|trans }}</h1>
            <div class="intro__info justify-content-center">
                <div class="mt-5 text-center">
                    <a class="btn btn-primary btn-big" href="{{ path('app_administration_users_dashboard') }}">
                        {{ 'administration.users.title'|trans }}
                    </a>
                </div>
                <div class="mt-5 text-center">
                    <a class="btn btn-primary text-center btn-big" href="{{ path('app_administration_match_dashboard') }}">
                        {{ 'administration.match.title'|trans }}
                    </a>
                </div>
                <div class="mt-5 text-center">
                    <a class="btn btn-primary text-center btn-big" href="{{ path('app_administration_drag_dashboard') }}">
                        {{ 'administration.drag.title'|trans }}
                    </a>
                </div>
            </div>
        </div>
    </section>

{% endblock %}