{% extends 'base.html.twig' %}

{% block title %}{{ 'administration.users.edit'|trans }}{% endblock %}

{% block body %}
    <section class="intro">
        <div class="container">
            <h1 class="intro__title">{{ 'administration.users.edit'|trans }}</h1>
            <div class="admin-game-detail">
                <div class="form">
                    {{ form_errors(form) }}

                    {{ form(form) }}
                </div>
            </div>
        </div>
    </section>

{% endblock %}