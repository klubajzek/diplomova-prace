{% extends 'base.html.twig' %}

{% block title %}Zapomenuté heslo{% endblock %}

{% block body %}
    <section class="intro">
        <div class="container">
            <h1 class="intro__title">Zapomenuté heslo</h1>
            <div class="intro__info">
                {#                <div class="intro__left">#}
                <div>
                    Na zadaný e-mail bylo odesláno obnovení hesla. Odkaz je platný po dobu {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.<br>
                    Pokud Vám nepřišel žádný e-mail, zkontrolujte prosím složku SPAM, případně to <a href="{{ path('app_forgot_password_request') }}">zkuste znovu</a>.
                </div>

                {#                    Pro více informací o této aplikaci navštivte sekci <a href="{{ path('app_about') }}">O aplikaci</a>.#}
                {#                    Pro více informací o zdrojích aplikace naštivtě sekci <a href="{{ path('app_about') }}">Zdroje</a>.#}
                {#                </div>#}
                {#                <div class="intro__right">#}
                {#                    <img src="{{ asset('assets/images/placeholder.svg') }}" alt="Lorem ipsum">#}
                {#                </div>#}
            </div>
        </div>
    </section>
{% endblock %}
