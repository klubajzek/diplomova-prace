{% extends 'base.html.twig' %}

{% block title %}Obnovení hesla{% endblock %}

{% block body %}
    <section class="intro">
        <div class="container">
            <h1 class="intro__title">Obnovení hesla</h1>
            <div class="admin-game-detail">

                <div class="form">
                    {{ form_start(resetForm) }}
                        {{ form_row(resetForm.plainPassword) }}
                        <button class="btn btn-primary">Uložit</button>
                    {{ form_end(resetForm) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
